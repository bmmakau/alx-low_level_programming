#include <stdio.h>
#include "main.h"

/**
  * print_rev - Prints string in reverse order
  * @s: string to print
  *
  * Return: void
  */
void print_rev(char *s)
{
	int x = 0;

	while (s[x] != '\0')
	{
		c++;
	}

	for (c -= 1; c >= 0; c--)
	{
		_putchar(s[c]);
	}

	_putchar('\n');
}
